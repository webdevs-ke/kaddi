import { Card } from "./card.model";
import { Pile } from './pile.model';


/**
 * LINKED-LIST OR Tableau (Pile object)
 * 
 * The Tableau LL allows us to also view the Card on top of the stack (head Node),
 * as opposed to the Deck & Hand (Pile objects).
 * 
 */

export class Tableau extends Pile {
  
}